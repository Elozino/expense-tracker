import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  avatarIncome: {
    color: "#fff",
    backgroundColor: "green",
  },
  avatarExpense: {
    color: "#fff",
    backgroundColor: "red",
  },
  list: {
    maxHeight: "150px",
    overflow: "auto",
  },
}));
